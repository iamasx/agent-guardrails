// API router — REST endpoints + SSE stream + SIWS auth for the dashboard.
// Routes and middleware will be wired in Phase 5-6.

import express from "express";

export const apiRouter: express.Router = express.Router();

// REST routes, SSE, and auth middleware will be mounted here in later phases
