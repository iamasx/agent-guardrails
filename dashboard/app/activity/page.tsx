import { AppShell } from "@/components/app-shell";

export default function ActivityPage() {
  return (
    <AppShell
      title="Activity"
      subtitle="Global guarded transactions and AI verdicts."
    >
      <div className="empty">Activity feed and realtime stream are delivered in later phases.</div>
    </AppShell>
  );
}
